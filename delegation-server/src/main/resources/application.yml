server:
  port: 32101
spring:
  application:
    name: delegation-server
eureka:
  instance:
    hostname: localhost
  # standalone mode
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:32001/eureka/
#minio
minio:
  endPoint: http://42.192.56.231:32004
  #  endPoint: http://localhost:32004
  accessKey: minioadmin
  secretKey: minioadmin
  # bucketName: minioTest
  partSize: 1048576000000

