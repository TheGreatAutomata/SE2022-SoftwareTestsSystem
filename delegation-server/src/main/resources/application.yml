server:
  port: 32101
spring:
  application:
    name: delegation-server
eureka:
  instance:
    hostname: localhost
  # standalone mode
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:32001/eureka/
#minio
minio:
  endPoint: http://42.192.56.231:9000
  accessKey: admin
  secretKey: password
# bucketName: minioTest
  partSize: 104857600

